# =============================================================================
# Dockerfile.infer-transformers — HF Transformers + PEFT inference (infer2.py)
# =============================================================================
# Dependencies only — clone your code at runtime.
#
# Base:   CUDA 13.0 + cuDNN devel on Ubuntu 24.04
# PyTorch: cu130 wheel (latest >=2.8.0)
#
# Lighter than the training image — no DeepSpeed, WandB, datasets, etc.
# Still needs devel image because causal-conv1d compiles CUDA extensions.
#
# Host requirements:
#   NVIDIA driver >= 575.51.03 (CUDA 13.0 minimum)
#   nvidia-container-toolkit installed
# =============================================================================

FROM nvidia/cuda:13.0.0-cudnn-devel-ubuntu24.04

# ---------- System packages ----------
ENV DEBIAN_FRONTEND=noninteractive
RUN apt-get update && apt-get install -y --no-install-recommends \
        python3 python3-pip python3-venv python3-dev \
        git wget curl \
        build-essential \
        ninja-build \
        vim \
    && ln -sf /usr/bin/python3 /usr/bin/python \
    && apt-get clean && rm -rf /var/lib/apt/lists/*

# ---------- CUDA env vars ----------
ENV CUDA_HOME=/usr/local/cuda
ENV PATH="${CUDA_HOME}/bin:${PATH}"
ENV LD_LIBRARY_PATH="${CUDA_HOME}/lib64:${LD_LIBRARY_PATH}"
ENV MAX_JOBS=8

WORKDIR /workspace

# ---------- PyTorch (CUDA 13.0) ----------
RUN pip install --no-cache-dir --break-system-packages \
        "torch>=2.8.0" \
        --index-url https://download.pytorch.org/whl/cu130

# ---------- Qwen3.5 Gated DeltaNet kernels ----------
RUN pip install --no-cache-dir --break-system-packages \
        "causal-conv1d @ git+https://github.com/Dao-AILab/causal-conv1d" \
    && pip install --no-cache-dir --break-system-packages \
        "flash-linear-attention @ git+https://github.com/fla-org/flash-linear-attention"

# ---------- Inference stack ----------
RUN pip install --no-cache-dir --break-system-packages \
        "transformers>=5.2.0,<5.3.0" \
        "peft>=0.18.0" \
        "accelerate>=1.0.0" \
        "pymupdf>=1.24.0" \
        "pyyaml>=6.0"

CMD ["bash"]
